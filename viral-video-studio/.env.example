# Local Ollama — no API key, no cost. Install from https://ollama.com
# Start the server with `ollama serve` and pull a model with `ollama pull llama3.1`.
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3.1
